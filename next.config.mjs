/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgur.com', "duckdevivery.s3.us-east-1.amazonaws.com","api.qrserver.com"],
    },
};

export default nextConfig;